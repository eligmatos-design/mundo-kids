# Render.com — deploy gratuito do servidor Mundo Kids
# Site: https://render.com

services:
  - type: web
    name: mundo-kids-server
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
